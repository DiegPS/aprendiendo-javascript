function largoDeArray(array) {
  // debe retornar el largo del array
  return
}
function agregarAlFinalDelArray(array, elemento) {
  // esta funcion recibe dos parametros el array
  // y el elemento que le agregaras al array
  return
}
function agregarAlInicioDelArray(array, elemento) {
  // esta funcion recibe dos parametros el array
  // y el elemento que le agregaras al array
  return
}
function eliminaAlFinalDelArray(array) {
  // debes eliminar un elemento del array
  return
}
function eliminaAlInicioDelArray(array) {
  // debes eliminar un elemento del array
  return
}

module.exports = {
  largoDeArray,
  agregarAlFinalDelArray,
  agregarAlInicioDelArray,
  eliminaAlFinalDelArray,
  eliminaAlInicioDelArray
}